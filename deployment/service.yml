kind: Service
apiVersion: v1
metadata:
  name: iam-production
spec:
  selector:
    app: iam
    env: production
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8000

---

kind: Service
apiVersion: v1
metadata:
  name: iam-staging
spec:
  selector:
    app: iam
    env: staging
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8000
